services:
  - type: web
    name: air-motion-canvas
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python webrtc_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
      - key: RENDER
        value: true
